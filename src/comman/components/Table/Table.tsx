import React, {FC, ReactNode} from 'react';

type TableType = {
    children: ReactNode
}

export const Table: FC<TableType> = ( {children }) => {
    return (
        <div>

        </div>
    );
};
